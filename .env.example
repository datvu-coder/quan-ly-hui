# Sao chép file này thành .env và điền giá trị thực
# cp .env.example .env

# Bí mật API — dùng chung cho server và client khi build
# Tạo chuỗi ngẫu nhiên: openssl rand -hex 32
API_SECRET=thay-bang-chuoi-ngau-nhien-dai-it-nhat-32-ky-tu
